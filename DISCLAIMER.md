# 免责声明 / Disclaimer

本项目（ZCode Skin Manager）是一个**社区第三方补丁**，通过解包并修改 ZCode 客户端的 `app.asar` 文件、向渲染进程注入前端脚本来实现换肤功能。使用前请务必阅读并理解以下风险。

## 风险声明

1. **非官方**：本项目与 ZCode 官方（zcode.z.ai）无关，未获得官方授权或认可。

2. **修改客户端文件**：安装补丁会修改 ZCode 安装目录下的 `app.asar` 文件。虽然脚本会先备份原文件（`app.asar.skinbak`），但任何对程序文件的修改都存在风险，包括：
   - 应用启动失败或运行异常
   - 与 ZCode 自动更新冲突（更新后补丁会被覆盖，需重新打补丁）
   - 极端情况下可能需要重装 ZCode

3. **隐私**：皮肤配置（壁纸路径、透明度等）保存在本地 `localStorage`，不会上传。但壁纸路径可能包含你的用户名等个人信息，请勿将导出的配置 JSON 分享给不信任的人。

4. **性能**：视频壁纸、动态特效（星空/飘雪/极光）会持续占用 CPU/GPU 资源，可能导致耗电增加、风扇高速运转。4K 视频壁纸尤为耗资源。

5. **稳定性**：本补丁依赖 ZCode 未公开的内部 CSS 变量（`--color-*`）和 `window.zcode.selectFile` 等内部 API。ZCode 升级后这些内部结构可能变化，导致补丁失效或界面异常。

6. **与其他补丁共存**：本补丁从当前 app.asar 解包并幂等注入，可与 zcode-account-switcher 等其他 asar 注入补丁共存。但多个补丁同时修改 app.asar 时，冲突排查更复杂；若某补丁异常，先用各自的 unpatch 脚本逐个还原再定位。

## 安全操作建议

- 打补丁前**务必完全退出 ZCode**（托盘图标右键退出，不是关窗口）
- 首次打补丁会自动备份 `app.asar.skinbak`，请勿删除，这是唯一还原依据
- 还原使用 `unpatch.bat`，会从备份恢复原始文件
- 若 ZCode 升级后出现异常，先运行 `unpatch.bat` 还原，再决定是否重新打补丁

## 许可

本项目采用 MIT 许可，但**不包含任何 ZCode 的版权文件**。补丁脚本仅注入自研的前端代码，不分发、不复制 ZCode 任何原始文件。`app.asar` 的修改完全在你本机进行。

**使用即表示你理解并接受上述全部风险。**
